<Window x:Class="ColorSys.WPF.Views.SettingView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ColorSys.WPF.Views"
        mc:Ignorable="d"
        Title="SettingView" Height="450" Width="800">
    <Border CornerRadius="8" BorderBrush="#FFCCCCCC" BorderThickness="1" Margin="8">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- 第一行：默认值 | 颜色空间 | 色差公式 -->
            <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,12">
                <CheckBox x:Name="ChkDefault" Content="默认值" VerticalAlignment="Center"/>
                <TextBlock Text="颜色空间：" Margin="25,0,5,0" VerticalAlignment="Center"/>
                <ComboBox x:Name="CbbColorSpace" Width="120" SelectedIndex="0">
                    <ComboBoxItem>CIE Lab</ComboBoxItem>
                </ComboBox>
                <TextBlock Text="色差公式：" Margin="15,0,5,0" VerticalAlignment="Center"/>
                <ComboBox x:Name="CbbDeltaE" Width="120" SelectedIndex="0">
                    <ComboBoxItem>△E*ab</ComboBoxItem>
                </ComboBox>
            </StackPanel>

            <!-- 第二行：列表标题 -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="80"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="可用数据项" FontWeight="SemiBold" HorizontalAlignment="Center" Grid.Column="0"/>
                <TextBlock Text="显示的数据项" FontWeight="SemiBold" HorizontalAlignment="Center" Grid.Column="2"/>
            </Grid>

            <!-- 第三行：两列表 + 箭头按钮 -->
            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="120"/>
                </Grid.ColumnDefinitions>

                <!-- 左侧：可用数据项 -->
                <ListBox x:Name="LstAvailable" Grid.Column="0" DisplayMemberPath="Name"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"/>

                <!-- 中间：箭头按钮 -->
                <StackPanel Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Button x:Name="BtnAdd"     Content="→" Width="90" Margin="0,10" Command="{Binding MoveCommand}" CommandParameter="Add"/>
                    <Button x:Name="BtnRemove"  Content="←" Width="90" Margin="0,10" Command="{Binding MoveCommand}" CommandParameter="Remove" />

                    <Button x:Name="BtnTop"     Content="全部移除" Width="90" Margin="0,10" Command="{Binding MoveCommand}" CommandParameter="DeleteAll" />
                </StackPanel>

                <!-- 右侧：显示的数据项 -->
                <ListBox x:Name="LstDisplay" Grid.Column="2" DisplayMemberPath="Name"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"/>
                <StackPanel Grid.Column="3" HorizontalAlignment="Center" VerticalAlignment="Center">
                    
                    <Button   Content="↑" Width="80" Margin="0,5" Command="{Binding MoveCommand}" CommandParameter="Up"/>
                    <Button   Content="↓" Width="80" Margin="0,5"  Command="{Binding MoveCommand}" CommandParameter="Down"/>
                    <Button   Content="置顶" Width="80" Margin="0,5"  Command="{Binding MoveCommand}" CommandParameter="Top"/>
                    <Button   Content="置底" Width="80" Margin="0,5"  Command="{Binding MoveCommand}" CommandParameter="Bottom"/>
                </StackPanel>

            </Grid>

            <!-- 第四行：按钮 -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="3" Margin="0,15,0,0">
                <Button Content="确定" Width="80" Margin="5,0" IsDefault="True" Command="{Binding SaveCommand}"/>
                <Button Content="恢复" Width="80" Margin="5,0" Command="{Binding RecoveCommand}"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
