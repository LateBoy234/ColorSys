<Window x:Class="ColorSys.WPF.Views.ConnectView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ColorSys.WPF.Views"
        xmlns:EnumConvert="clr-namespace:ColorSys.Permission.ValueRules;assembly=ColorSys.Permission"
        mc:Ignorable="d"
        Title="ConnectView" Height="450" Width="800">
    <Window.Resources>
        <EnumConvert:EnumDescriptionConverter x:Key="EnumDescConverter"/>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="36"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Vertical" HorizontalAlignment="Center" Grid.Row="1" Margin="5">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <StackPanel Orientation="Horizontal" Grid.Row="0">
                    <Label Content="仪器类型："/>
                    <ComboBox ItemsSource="{Binding DeviceTypeList}" SelectedItem="{Binding SelectDeviceType}"  HorizontalAlignment="Center" Margin="5">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Converter={StaticResource EnumDescConverter}}"/>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Grid.Row="1">
                    <Label Content="连接方式："/>
                    <ComboBox ItemsSource="{Binding CommunicationTypeList}" SelectedItem="{Binding SelectCommunicationType}"  HorizontalAlignment="Center" Margin="5">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Converter={StaticResource EnumDescConverter}}"/>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                </StackPanel>
                <ContentControl Content="{Binding SerialOptions}" Grid.Row="2">
                    <ContentControl.ContentTemplate>
                        <DataTemplate>
                            <StackPanel>
                                <Grid Name="netSocketTypy" Visibility="Collapsed" Margin="5">
                                    <Grid.RowDefinitions>
                                        <RowDefinition/>
                                        <RowDefinition/>
                                    </Grid.RowDefinitions>
                                    <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="5">
                                        <TextBlock Text="IP："/>
                                        <TextBox Text="{Binding IP}" MinWidth="100"/>
                                    </StackPanel >
                                    <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="5">
                                        <TextBlock Text="Port："/>
                                        <TextBox Text="{Binding Port}" MinWidth="80"/>
                                    </StackPanel>
                                </Grid>
                                <Grid Name="usbType" Visibility="Collapsed" Margin="5">
                                    <StackPanel Orientation="Vertical">
                                        <StackPanel Orientation="Horizontal" Margin="10">
                                            <TextBlock Text="端口号：" Margin="5"/>
                                            <ComboBox ItemsSource="{Binding SerialPortList}" SelectedItem="{Binding SelectedSerialPort}" HorizontalAlignment="Center" Margin="5" >
                                               
                                            </ComboBox>
                                        </StackPanel>
                                        <StackPanel Orientation="Horizontal" Margin="10">
                                            <TextBlock Text="波特率：" Margin="5"/>
                                            <ComboBox ItemsSource="{Binding BaudRateList}" SelectedItem="{Binding SelectedBaudRate}" HorizontalAlignment="Center" Margin="5">
                                                <ComboBox.ItemTemplate>
                                                    <DataTemplate>
                                                        <TextBlock Text="{Binding Converter={StaticResource EnumDescConverter}}"/>
                                                    </DataTemplate>
                                                </ComboBox.ItemTemplate>
                                            </ComboBox>
                                        </StackPanel>
                                        <StackPanel Orientation="Horizontal" Margin="10">
                                            <TextBlock Text="数据位：" Margin="5"/>
                                            <ComboBox ItemsSource="{Binding DateBitList}" SelectedItem="{Binding SelectedDateBit}" HorizontalAlignment="Center" Margin="5">
                                                <ComboBox.ItemTemplate>
                                                    <DataTemplate>
                                                        <TextBlock Text="{Binding Converter={StaticResource EnumDescConverter}}"/>
                                                    </DataTemplate>
                                                </ComboBox.ItemTemplate>
                                            </ComboBox>
                                        </StackPanel>
                                        <StackPanel Orientation="Horizontal" Margin="10">
                                            <TextBlock Text="校验位：" Margin="5"/>
                                            <ComboBox ItemsSource="{Binding ParityBitList}" SelectedItem="{Binding SelectedParityBit}" HorizontalAlignment="Center" Margin="5">
                                                <ComboBox.ItemTemplate>
                                                    <DataTemplate>
                                                        <TextBlock Text="{Binding Converter={StaticResource EnumDescConverter}}"/>
                                                    </DataTemplate>
                                                </ComboBox.ItemTemplate>
                                            </ComboBox>
                                        </StackPanel>
                                        <StackPanel Orientation="Horizontal" Margin="10">
                                            <TextBlock Text="停止位：" Margin="5"/>
                                            <ComboBox ItemsSource="{Binding StopBitList}" SelectedItem="{Binding SelectedStopBit}" HorizontalAlignment="Center" Margin="5">
                                                <ComboBox.ItemTemplate>
                                                    <DataTemplate>
                                                        <TextBlock Text="{Binding Converter={StaticResource EnumDescConverter}}"/>
                                                    </DataTemplate>
                                                </ComboBox.ItemTemplate>
                                            </ComboBox>
                                        </StackPanel>
                                    </StackPanel>
                                </Grid>
                            </StackPanel>
                            <DataTemplate.Triggers>
                                <DataTrigger Binding="{Binding ParentCommType}" Value="TCP">
                                    <Setter TargetName="netSocketTypy" Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding ParentCommType}" Value="USB">
                                    <Setter TargetName="usbType" Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </DataTemplate.Triggers>
                        </DataTemplate>
                    </ContentControl.ContentTemplate>
                </ContentControl>
                <StackPanel Orientation="Horizontal" Margin="5" Grid.Row="3">
                    <Button Content="确定" Command="{Binding ConnectCommand}"  CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}" Margin="5"/>
                    <Button Content="取消" Command="{Binding CancelCommand}"  CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}" Margin="5"/>
                </StackPanel>
            </Grid>
        </StackPanel>
        
    </Grid>
</Window>
