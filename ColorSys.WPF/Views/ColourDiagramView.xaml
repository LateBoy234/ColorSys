<UserControl x:Class="ColorSys.WPF.Views.ColourDiagramView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:oxy="http://oxyplot.org/wpf"
             xmlns:local="clr-namespace:ColorSys.WPF.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="4*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Border Grid.Column="0" >
            <oxy:PlotView Model="{Binding XyAxisModel}">
            </oxy:PlotView>
        </Border>
        <!--<Border Grid.Column="1" BorderBrush="Transparent" BorderThickness="1" Margin="10">
            <oxy:PlotView Model="{Binding XyAxisModel}">
            </oxy:PlotView>
        </Border>-->
    </Grid>
</UserControl>
